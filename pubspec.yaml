name: mobile
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_bloc: ^4.0.0
  bloc: ^4.0.0
  http: ^0.12.1
  equatable: ^1.1.1
  meta: ^1.1.8
  dio: ^3.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^5.0.0
  mockito: ^4.1.1
  pedantic_mono: any

flutter:

  uses-material-design: true
